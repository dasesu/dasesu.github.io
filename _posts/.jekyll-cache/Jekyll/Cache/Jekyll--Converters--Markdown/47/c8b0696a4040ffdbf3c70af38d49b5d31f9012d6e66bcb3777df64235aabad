I"�<h2 id="install-rvm">Install RVM</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sudo apt-get install software-properties-common

sudo apt-add-repository -y ppa:rael-gc/rvm
sudo apt-get update
sudo apt-get install rvm

sudo usermod -a -G rvm &lt;yourusername&gt;

</code></pre></div></div>

<p>Luego agregar la opcion <code class="language-plaintext highlighter-rouge">run command as a login shell</code> a la terminal window, click Edit &gt; Profile Preferences, click on Title and Command tab and check Run command as login shell.</p>

<p align="center">
<img src="/assets/img/posts/rvm/login.png?raw=true" />
</p>

<p>Reboot</p>

<h2 id="instalar-diferentes-versiones-de-ruby">Instalar diferentes versiones de Ruby</h2>

<p>Si quueremos agregar una nueva versión de ruby</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rvm install 2.1.0
</code></pre></div></div>

<p>Si queremos crear un entorno</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rvm gemset create &lt;nombre_del_gemset&gt;
</code></pre></div></div>

<p>Si queremos usar una versión o un entorno</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rvm use 2.1.0

rvm gemset use &lt;nombre_del_gemset&gt;
</code></pre></div></div>

<p>por ejemplo:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rvm install 2.1.0 
rvm gemset create envios
rvm gemset use 2.1.0@envios
</code></pre></div></div>

<p>Para usar una versión especifica por defecto</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rvm --default use 2.7
</code></pre></div></div>

<p>Para listar las versiones instaladas</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> rvm list

 rvm list rubies
</code></pre></div></div>

<p>Si queremos verificar cual estamos usando por defecto</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rvm list default
</code></pre></div></div>
:ET